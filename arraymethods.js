

let data = [5,6,8,2,9];

// console.log(data.push(6)); // adding element to the last
// console.log(data.unshift(1)); // adding element to the first
// console.log(data.pop()); // removing element at the last
// console.log(data.shift());  // removing element in the first

console.log(data.slice(1, 2)); // first values indicates the index, second values no .of elements you want to remove

console.log(data);